name: CI

on:
  push:
    branches:
      - master
      - v1
  pull_request:
    branches:
      - master
      - v1

jobs:
  nightly:
    name: CI Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x, 14.x]
    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install dependencies
        run: |
          npm ci
      - name: Lint
        run: |
          npm run lint
      - name: Test
        run: |
          npm test
      - name: Build
        run: |
          npm run build
        env:
          CI: true
      - name: Install CLI tool
        run: |
          npm install --global .
      - name: Bootstrap userscript
        working-directory: ${{ runner.temp }}
        run: |
          mkdir bootstrapped-userscript
          cd $_
          userscripter init
          npm install
          npm run build
  userscripts:
    name: Dependent Userscripts
    runs-on: ubuntu-latest
    strategy:
      matrix:
        userscript:
          - SimonAlling/better-sweclockers
        node-version: [12.x, 14.x]
      fail-fast: false
    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install dependencies
        run: |
          npm ci
      - name: Build
        run: |
          npm run build
      - name: Pack
        id: pack
        run: |
          echo "::set-output name=tarball::$(pwd)/$(npm pack)"
      - name: Clone userscript
        uses: actions/checkout@v2
        with:
          repository: ${{ matrix.repo }}
          path: dependent-userscripts/${{ matrix.userscript }} # Must be relative to github.workspace, apparently.
      - name: Move userscript
        run: |
          mv "${{ github.workspace }}/dependent-userscripts/${{ matrix.userscript }}" "${{ runner.temp }}/${{ matrix.userscript }}"
      - name: Install userscript dependencies
        working-directory: ${{ runner.temp }}/${{ matrix.userscript }}
        run: |
          npm ci
      - name: Install Userscripter
        working-directory: ${{ runner.temp }}/${{ matrix.userscript }}
        run: |
          pwd
          ls -al
          npm install "${{ steps.pack.outputs.tarball }}"
      - name: Build userscript
        working-directory: ${{ runner.temp }}/${{ matrix.userscript }}
        run: |
          npm run build
